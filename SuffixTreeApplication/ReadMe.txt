This code uses a suffix tree to analyze a given string. It is called from the command line with parameters:
 <test executable> <input file containing sequence s> <input alphabet file>
 Example files demonstrating sequence format and alphabet format can also be seen.